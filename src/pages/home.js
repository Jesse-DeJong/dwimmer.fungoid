const Home = () => {
    
    // Call renderImage() on load and when required by a click or other state change such as code snippet menu, host the top level state variables for menus etc to be passed down to the renderImage() component to pass down to that states component so it can change with it

    return (
        <h1>HOMEPAGE</h1>
        )

}

export default Home;